feat(security): integrate full Android Device Administrator enforcement

Implemented an 8-tier hardware security gate by integrating Device Administrator privileges to prevent unauthorized app removal.

- Native (Android): Registered 'BlockerDeviceAdminReceiver' and implemented 'isAdminActive' check in EnforcementModule.kt.
- Native (Samsung/OneUI): Optimized settings redirect to target the 'Device Admin Apps' list directly via explicit ComponentName intent.
- Hook (usePermissions): Integrated 'admin' status into the reactive auditing engine.
- UI (Permissions): Added a new interactive permission card for Device Admin with real-time state synchronization.
- Enforcement: Hardened the 'Commit' gate in final.tsx and the main dashboard warning to strictly require Admin activation before allowing users to bind new commitments.
