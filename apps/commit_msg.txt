style(native-ui): harmonize semantic red across commitment confirmation modals

- Updated 'Keep current' (EventDetailLocation) and 'Discard' (BlocklistView) buttons to use semantic red (#FF3B30).
- Synchronized the Lock Error acknowledgement button in BlocklistView with the same red palette.
- Ensured visual consistency for destructive and cancellation actions across the commitment editing workflow.
