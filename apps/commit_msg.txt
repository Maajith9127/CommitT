refactor(notifications): refine feed UX and document dogfooding debt

- Pushed the WebSocket `ActivityIndicator` loader down below the structural `TabsBar` layer to ensure instant navigation paints without full-screen layout flashing.
- Rewrote the `Upcoming` tab row templates to parse the user's custom task `description` inline, stripping redundant generic scheduling text.
- Added explicit `Ends At:` chronological markers onto the feed UI mapping natively against the `_live_schedule_time` backend queue bomb.
- Stripped informal conversational comments and replaced them with rigorous JSDoc `@remarks` parameters.
- Formally delineated temporary `Dogfooding Implementation` complexities (system table scraping) vs explicitly scoped `Production Migration Path` roadmapping.
