feat(native): full per-slot accountability integration & UX refinement

- **End-to-End Integration**: Fully wired `LocationPresetPickerModal` and `DigitalPresetPickerModal` to individual time slots, enabling persistent per-slot condition states.
- **Dynamic UI Logic**: 
    - Refactored `TimeSlotCard` with ref-based height remeasurement for jitter-free Reanimated transitions that adapt to multi-line addresses.
    - Added a horizontal scrollable strip of app icons (28x28) in the App Block row, resolved instantly via `useAppStore`.
- **UX & Control System**:
    - Isolated selection interactivity to the `VerificationStatusCircle` to prevent accidental choosing while scrolling presets.
    - Updated picker modals to remain open during selection/deselection for rapid review, closing only on explicit user dismissal.
    - Increased modal heights to 90% for a more expansive list and map viewing area.
- **Design & Layout Polish**:
    - Standardized picker headers with properly aligned `HeaderTitle` and iconography.
    - Implemented smart bottom spacing in the schedule view to ensure expanded card content is never clipped by the global footer.
- **Performance**: Maintained the "Instant Recall" zero-latency data flow from the pre-hydrated Zustand store to all picker components.
