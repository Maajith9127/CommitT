feat(accountability): fix persistence and validation of penalty/waiver contracts

- Update Convex API and core service to support updating accountability contracts (penalty/waiver).
- Implement domain-level validation for waivers, including CAPTCHA count and deadline checks.
- Fix "packing" bug in useCommitTask hook where penalty/waiver data was omitted during task updates.
- Refine CAPTCHA setup UI to preserve existing deadlines and add "1 hour" option to settings.
- Ensure atomic synchronization of modified accountability rules across cloud and local cache.


wsl.exe --shutdown
