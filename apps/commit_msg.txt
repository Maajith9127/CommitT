feat(security/logging): implement native adb hardening and centralized audit ledger

Security Hardening:
- Added `isUsbDebuggingEnabled` to BlockerModule (Android Native) to detect ADB via system settings, bypassing "Ghost ADB" menu-hiding tricks.
- Integrated the hardware check into SecurityShield for app-wide lockdown when debugging is active.

Audit Logging System:
- Created a production-grade `auditLogs` ledger in Convex with compound indexing for O(1) retrieval.
- Integrated success/failure logging into `runVerification`, `firePenaltyAction`, and the `embarrassing_photo` executor.
- Established a type-safe `auditEventTypeEnum` for strict event classification.

Frontend Implementation:
- Transformed the notifications "Verified" tab into a "History" chronological feed with dynamic status icons and color-coded timestamps.
- Implemented the "Clear History" feature in Profile settings using a `ConfirmationModal` and secured Convex mutation.
- Standardized backend logging headers to use clean ** ** annotations without emojis.
