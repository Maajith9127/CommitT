feat(security): implement "Ironclad Marriage" hardware-account monogamy core

- Re-architected hardware enforcement to use a strictly bonded 1-to-1 marriage model.
- Implemented "Intelligent Proxy-Divorce" in `bondService` to allow seamless device 
  migration only when the user has zero future task commitments.
- Refactored `syncHardwareBond` API to use a polling-retry mechanism, resolving 
  Convex session synchronization race conditions during the Google Login flow.
- Hardened `AuthForm` to execute immediate session purging and Google sign-out if 
  hardware enforcement fails, preventing bypasses.
- Simplified Profile UI by removing legacy logout safety checks in favor of terminal 
  identity verification at the login gateway.
- Added production-grade documentation and enforcement logic for long-term scalability.

"No escape once the commitment is made." 🛡️🔒💎
