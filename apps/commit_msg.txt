feat(native/blocker): escalate settings tampering to hardware screen lock

- Implement `escalateToDeviceLock` helper to manage severe security violations.
- Orchestrate a two-factor neutralization sequence: executes a hardware 
  `GLOBAL_ACTION_LOCK_SCREEN` to instantly turn off the device display, 
  followed by enqueueing the `BlockerOverlayActivity`.
- Refactor `performDeepSecurityAudit` to route all critical settings bypass
  attempts (Time Travel, Dev Options, Accessibility, Factory Reset) through 
  the new escalation protocol.
- Add architectural comments detailing the psychological and technical design 
  decisions behind hardware-level context breaks versus UI-level overlays.

Impact: Severely limits circumvention opportunities by actively breaking the 
user's flow at the OS level upon detection of malicious navigation intents.
