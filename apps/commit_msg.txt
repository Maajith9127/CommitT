feat(sync): expand Imperial Forward-Heal strategy to all event-level sagas

- Hardened `schedules.tsx` (Temporal Shifts) with infinite-retry recovery loops
- Upgraded `useTaskActions.ts` to support Imperial Deletion of individual instances
- Migrated `EventDetailLocation.tsx` (Geofence Pivots) to the Cloud-First model
- Integrated Imperial Forward-Heal into `BlocklistView.tsx` for digital blocklist updates
- Standardized recovery flow: Sync Engine (Delta Ingestion) + Hardware Alarm Re-alignment
- Implemented global `HealOverlay` gating across all instance-level state transitions
- Simplified recovery loops to rely strictly on Cloud source-of-truth, eliminating fragile manual patches
