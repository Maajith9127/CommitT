refactor(location): optimize and modularize the geofencing map logic

- Implemented Buffered Commit strategy for geofence radius updates (60fps feedback).
- Memoized geofence visual calculations (Polygons/Circles) using `useMemo`.
- Stabilized interaction handlers with `useCallback` to prevent UI jitter.
- Refactored screen architecture into a clean 6-phase pipeline (State -> Sensors -> Visuals -> Handlers -> Render).
- Added comprehensive production-grade documentation for the inverse geofencing logic.
- Simplified Platform Guard and visual layer synchronization.
