feat(ui): harden Presets Hub and Notifications with design system tokens

- **Presets Hub**: Standardized "Rule DNA" chips with THEME tokens, increasing data density and visual parity with picker modals.
- **Notifications**: Migrated feed to Obsidian design system, implementing saffron highlights for upcoming deadlines and semantic color synchronization for history items.
- **Input Components**: Fixed style-merging logic in `Input` to prevent theme color overrides and enforced white text visibility in dark mode across all editing flows (Digital, Location, Rule).
- **TabsBar**: Standardized typography and active-state highlights using theme-compliant Saffron tokens.
- **Visual Polish**: Unified backgrounds using `pureBlack` and `background` tokens and hardened component borders across the alerts and presets dashboards.
