feat(ui): harden email setup screen with design system tokens

- **Layout**: Migrated global backgrounds to `pureBlack` and `background` tokens.
- **Fields**: Standardized field separators and input borders using `surfaceElevated` tokens.
- **Typography**: Unified all labels, placeholders, and test-preview text to `textMuted`.
- **Surfaces**: Optimized the image preview and test sandbox card with `surface` backgrounds, themed borders, and `radii` tokens.
- **Icons**: Standardized header and action iconography to match the design system's text palette.
