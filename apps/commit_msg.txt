refactor(create-commit): decompose FinalScreen and elevate documentation

ARCHITECTURAL EXTRACTION:
- Extracted types, interfaces, and all constant objects (CONDITION_CONFIGS,
  LAYOUT, COLORS, SETTINGS_OPTIONS) into `_lib/constants.ts`.
- Extracted the three useMemo settings schema builders (Commitment Type,
  Alarms, Waiver Rules) into `_lib/useSettingsItems.ts` custom hook.
- Extracted penalty and waiver display resolvers into pure functions
  in `_lib/displayHelpers.ts`, replacing inline IIFEs in JSX.
- All extracted modules live under `_lib/` (Expo Router exclusion via
  underscore prefix convention).

DOCUMENTATION UPGRADE:
- Replaced all amateur single-line `//` comments with production-grade
  JSDoc block comments (`/** */`) throughout FinalScreen.
- Each logical section now carries detailed design rationale annotations
  covering architecture decisions, security model, and failure semantics.
- JSX section markers standardized to `{/* ── Section: Name ── */}` format.

IMPACT:
- FinalScreen reduced from 822 → ~450 lines (pure orchestration + JSX).
- Zero functional changes — exact behavioral parity maintained.
