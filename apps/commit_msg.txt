feat(header): refactor schedules navbar to match premium calendar standards

Refined the global sticky header in 

_layout.tsx
 to implement a high-density, functional aesthetic inspired by Google Calendar.

Key Changes:

Navigation UX: Replaced the full date string with a dynamic "Month-only" title (e.g., "March") and added a chevron-down toggle to signal DatePicker interaction.
Icon Standardization: Unified the scaling of all header actions (Hamburger Menu, Search, and Today) to 26px, perfectly synchronizing with the bottom TabBar design system.
Dynamic Avatar: Integrated a prominent w-10 h-10 user avatar that intelligently loads the session image with a robust fallback to the user's first initial.
Aesthetic Refinement: Restored the calendar-today action icon and ensured solid white color consistency across all header elements for a clean, high-contrast look.
Layout Optimization: Adjusted component gaps and touch targets to ensure standard mobile interaction guidelines are met.