perf(native): optimize navigation transitions and harden email delivery

- Eliminate UI jitter and black-screen flashes on Android swipe-back by setting native windowBackground to #000000 and disabling the O(N^2) disk-logging polyfill.
- Harden penalty emails against spam filters by using native MIME attachments (Resend API) instead of raw URLs and personalizing the sender identity to "Maajith".
- Implement a 5-second short-circuit delay in the accountability arming logic for rapid production testing.
