feat(presets): implement full location preset editing flow

- Added `updateLocationPreset` mutation with user ownership validation.
- Created `usePresetEditStore` bridge to decouple preset editing from the main task creation floor.
- Implemented `edit-location-preset.tsx` with editable name, radius slider, and search integration.
- Added `search-preset-location.tsx` to allow searching for new coordinates during the edit flow.
- Enhanced navigation transitions for the `(create-commit)` group to use a smooth 450ms fade.
