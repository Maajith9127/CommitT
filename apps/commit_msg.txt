feat(native): implement high-fidelity digital commitment edit flow

- Replicated the 'choose.tsx' creation UI within the EventDetailModal singleton to provide a 1:1 consistent instance-editing experience.
- Implemented a two-stage 'Confirmation → Action' pipeline for Blocklist modifications, mirroring the premium Waiver architecture.
- Synchronized blocklist updates with both the Convex backend and the local Android SQLite database for immediate enforcer reactivity.
- Optimized the 'BlocklistView' using the ActionScreenLayout to ensure pixel-perfect alignment of headers, tabs, and footer zones.
- Silenced high-frequency rendering logs in the temporal metadata component to clarify terminal output during debugging.
