style(navigation): restore native slide transitions and full-screen layout

Fixes the navigation regression in the create-commit flow to ensure consistent 
branding and native-feel transitions.

- Removed explicit 'fade' animation override for (create-commit) in root _layout.tsx.
- Re-enabled global 'slide_from_right' behavior for the commitment creation stack.
- Removed 'transparentModal' presentation to eliminate the system-level 
  border/overlay effect.
- Enforced 'black' backgroundColor via contentStyle for a seamless, 
  immersion-preserving transition.
