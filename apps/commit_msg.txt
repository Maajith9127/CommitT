refactor(core): implement hierarchical binding validation for slot-specific overrides

- [Architectural] Implemented the "Binding Action Protocol" across frontend & backend.
- [Validation] Added recursive schedule scanning to allow "Sparse Master" mode (validating tasks that have empty global conditions but protected time slots).
- [Logic] Enforced "Total Override" strategy: slot-specific conditions now completely supersede root conditions for that window to ensure clean enforcement states.
- [UI/UX] Decoupled final screen carousel state from granular validation to maintain "Global-Only" visibility while keeping the "Commit" flow active for valid sparse schedules.
- [Reliability] Standardized enforcer registries and resolved TypeScript readonly includes mismatches in local validation libraries.
