feat(native): implement penalty waiver configuration on final confirmation screen

- Add 'Waiver Rules' section to 'final.tsx' using the standard 'SettingsToggleCard' pattern for UX consistency.
- Implement 'Waiver Deadline' configuration allowing users to select grace periods (5h, 10h, 24h, 2d) for penalty mitigation.
- Add 'Allow Early Waiver' toggle to the draft state, permitting survival challenges to be attempted prior to commitment failure.
- Refactor 'TaskDraft' store schema to achieve parity with backend 'PenaltyWaiverSchema', adding 'deadline_minutes' and updating type discriminators to 'captcha' | 'paragraph'.
- Synchronize UI selection options with internal minute-based duration logic for robust backend persistence.
