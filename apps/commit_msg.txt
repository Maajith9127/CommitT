feat(native): per-slot accountability infrastructure & animated UI refactor

- Refactored `EventDetailModal` into a modular "Shell/Brain" architecture using the new `BaseDrawerModal` and `useEventDetail` hook.
- Implemented an "Instant Recall" preset system with `usePresetStore` (Zustand) and `usePresetHydration` for one-shot parallel data fetching.
- Built production-grade `LocationPresetPickerModal` and `DigitalPresetPickerModal` with selection indicators, map loading skeletons, and app icon galleries.
- Redesigned `TimeSlotCard` with a premium vertical-stack layout and buttery-smooth slide-down animations powered by `react-native-reanimated`.
- Standardized visual language across the commitment flow with 2px black separators, consistent icon colors (#9CA3AF), and library-standard text components.
