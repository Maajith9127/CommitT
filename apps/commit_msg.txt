style: tokenize TimeSet flow and schedule picker components

Refactored the commitment scheduling UI to align with the Saffron design system, removing all remaining legacy blue and hardcoded gray values.

Key Changes:
- Screen Theming: Migrated `time-set.tsx` to use `THEME.colors.pureBlack` for the layout background and Saffron accents for the screen title and repeat toggle.
- Component Extraction: Updated `DaySelector` to utilize theme-based background and text tokens for both active and inactive day bubbles.
- Picker Overhaul: Refactored `TimePicker` modal to utilize `THEME.colors.surface` for the card body and matched inactive tab backgrounds for a seamless visual flow.
- Slot Card Polish: Hardened `TimeSlotCard` by mapping its interactive elements, borders, and drawer sub-sections (Location, App Block, Rules) to central theme spacing and color tokens.
- Typography: Swapped all hardcoded `text-gray-400` utility classes within the time flow for the `THEME.colors.textMuted` token.

This ensures that the entire scheduling experience remains visually consistent with the primary commitment dashboard.
