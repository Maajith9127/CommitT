feat(ui): implement behavioral manifest in EventDetailModal

Standardized the display of commitment "DNA" (type, alarms, and grace periods) 
by decoupling operational configuration from the temporal view.

Visual Refinements:
- Extracted Type, Alarm, and Grace metadata into a dedicated `ConfigSection` 
  rendered immediately below the time window for better logical separation.
- Implemented a clean horizontal layout (Label left, Value right) to maximize 
  white space and improve readability on mobile screens.
- Standardized icon metrics to `size={28}` and `margin={12}`, achieving 1:1 
  visual parity with the primary countdown clock for a unified "Hero" column.
- Mapped contextual icons: `bullseye-arrow`/`repeat` for Task Type, `alarm` 
  for Schedule, and `timer-sand` for Grace buffer.

This restructuring declutters the Timer section while ensuring critical 
behavioral rules remain highly visible at the top of the detail drawer.
