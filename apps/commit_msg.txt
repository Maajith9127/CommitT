feat(profile): implement scorched-earth session management & imperial wipe engine

- **Scorched-Earth Logout Sequence**: 
  - Wired `ConfirmationModal` to `authClient.signOut()`.
  - Added `GoogleSignin.signOut()` to natively zero-out Google Play Services identity cache, aggressively preventing auto-login bypasses.
  - Implemented async spinner UI block during the termination phase to prevent dual-routing.

- **Full Device Resynchronization (Imperial Wipe)**:
  - Added a "Full Resync" option under "Session & Data".
  - Architected a manual Convex Delta trigger using `nukeAndRebuildSchema`. By explicitly using `DROP TABLE IF EXISTS` instead of `DELETE FROM`, this permanently bypasses the dreaded `NativeStatement.finalizeAsync` (SQLITE_CORRUPT) WAL pointer fragmentation bug.
  - Deletes the `sync_token` from SecureStore enforcing an 'Amnesia Wipe' that downloads 100% cloud parity state back into SQLite.

- **Graceful Error Bubbling**:
  - Wired up `ConfirmationModal` to natively handle `isLoading` tracking.
  - Attached a dynamic, styled `resyncError` payload to the modal children slot so background SQLite panics render inline rather than silently swallowing UI execution.

- **Architectural Polish**:
  - Restructured `profile.tsx` visual headers for clarity ("Session & Data" grouping).
  - Validated Global Mutex (`syncLock.execute`) bindings explicitly block `HydrationEngine` concurrency races during AppState transitions.
