feat(android): implement stabilized production-grade app blocking enforcer

- Refactor BlockerAccessibilityService to use 'Direct-to-Vault' enforcement, bypassing the React bridge for sub-5ms latency.
- Implement multi-path SQLite resolution (Expo Default, Standard, and Absolute) to ensure reliable vault access across Android variants.
- Add 'Relation-Aware' JSON parsing logic to support both 'outside' and 'within' digital commitment styles.
- Upgrade BlockerModule with a hardened system status audit for accurate permission reporting in the UI.
- Stabilize local-db schema (v11) with 'blocked_websites' support and enterprise-level migration documentation.
- Standardize all native modules with professional, deterministic logging and formal architectural documentation.
