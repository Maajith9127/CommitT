feat(protocol): implement 'Lightweight Protocol' for digital-only commitments

Optimized the accountability flow for tasks requiring only digital app-blocking (no location) to reduce database overhead and improve the user experience.

- Backend (Core): Implemented checkpoint-skipping in `createOne` and `update` services for digital-only tasks.
- Backend (Grading): Updated `runner.ts` to treat instances with no checkpoints as "Success by Default" while maintaining strictness for reported violations.
- Native UI: Enhanced status calculation in `useEventDetail` to display "Proceeded" for digital-only tasks by evaluating top-level condition states.
- Native UI: Fixed a bug in `time-set.tsx` where editing a time slot would destructively wipe out attached conditions (location/blocklists).
- State Consistency: Ensuring digital conditions are initialized to "verified" during rescheduling to provide a seamless "Proceeded" UI state.
