feat(presets): implement location tab and navigation restructuring

- Renamed 'Strict Mode' to 'Presets' in the bottom navigation bar and updated the icon to `options-outline`.
- Restructured `(main)/_layout.tsx` to include the new `/presets` route in the header configuration.
- Refactored `app/(main)/presets.tsx` with a three-tabbed interface: Location, Blocks, and Photos.
- Integrated `getRecommendedLocations` from the Convex backend to populate the Location tab.
- Reused the `LocationPresetCard` component with support for staggered map loading and hybrid map views.
- Implemented `ActionMenu` integration for preset management (deletion with ConfirmationModal).
