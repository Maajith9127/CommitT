fix(native-ui): decouple navigation from scrollable digital commit items

- Remove the all-encompassing Pressable wrapper from the CommitCard to prevent gesture conflicts.
- Refactor DigitalCommitItem to wrap only the header row (title and icons) in a Pressable.
- Ensure the application ScrollView is independent of the navigation trigger for smoother horizontal scrolling.
- This allows users to swipe through blocked apps without accidentally triggering a transition to the selection screen.
