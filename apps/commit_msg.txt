feat(native/ui): transition preset pickers to live backend subscriptions

- Migrate `LocationPresetPickerModal` and `DigitalPresetPickerModal` to live Convex `useQuery` subscriptions.
- Implement real-time "Recommended Locations" and "Digital Commitments" syncing directly from the backend.
- Fix monorepo-wide API import resolution by correctly utilizing the `@commit/backend` alias.
- Add robust loading guards and optional chaining to prevent crashes during initial data hydration.
- Refine interaction flow: restrict selection to the `VerificationStatusCircle` and maintain persistent modal states for seamless multi-preset toggling.
