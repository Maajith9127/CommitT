style(ui): theme location search flow and shared input primitives

Finalized the design system migration for the location discovery flow and hardened the shared Input component for application-wide consistency.

Key Changes:
- Shared Input Hardening: Refactored `Input.tsx` to utilize `surfaceElevated` and `textMuted` tokens, ensuring all search and form fields across the app share the same Obsidian aesthetic.
- Search Flow Migration: Tokenized `searchpac.tsx` to use `pureBlack` for the root container and `primary` (Saffron) for navigation icons.
- List Item Refinement: Updated location suggestion rows to use `textMain` and `textMuted` for typographic hierarchy, with border radii now bound to `THEME.radii.lg`.
- Layout Consistency: Patched `ScreenContainer.tsx` to support the `style` prop, enabling pixel-perfect background color management via theme tokens.
- Navigation: Standardized the back button in the search flow to use `surfaceElevated` backgrounds with Saffron tints.

This completes the visual integration of the location selection and search modules into the Saffron design language.
