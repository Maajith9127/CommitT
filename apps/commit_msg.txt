feat(native): implement intelligent map camera sync in location preset editor

- Add threshold-based synchronization logic to `EditLocationPresetScreen` to detect 
  external location updates (e.g., from the search results page).
- Prevent "camera fighting" by only auto-panning when the user is not actively 
  interacting with the map.
- Implement an 800ms high-performance animation for camera transitions to ensure 
  a premium UX.
- Resolve multiple TypeScript `implicitAny` lint errors in Zustand selectors and 
  state update callbacks.
- Add granular documentation for the coordinate synchronization strategy and 
  epsilon thresholding.

Fixes: Map camera position not updating after selecting a location via search.
