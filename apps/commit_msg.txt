feat(navigation): replace 'Strict Mode' with 'Presets' in bottom navigation

- Renamed the 'Strict' tab to 'Presets' in the bottom navigation bar.
- Updated the tab icon from lock-closed to options-outline for better semantics.
- Refactored (main)/_layout.tsx to handle the new /presets route and header title.
- Created the initial Presets screen component in app/(main)/presets.tsx.
