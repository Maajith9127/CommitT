fix(native/enforcer): implement real-time permission auditing and UI reactivity

- Replaces hardcoded 'true' values in EnforcementModule.kt with actual 
  Android system audits for Location, Camera, Notifications, and Alarms.
- Fixes Permission Card reactivity in CommitsScreen by adding 'permissions' 
  and 'listData' to useMemo/useCallback dependency arrays.
- Adds 'camera' to the critical enforcement readiness (isReady) check.
- Ensures the UI correctly reflects permission revocation/granting upon 
  app foregrounding.
