design(ui): harden Saffron & Obsidian system and tokenize selection components

- Refined the core 'Hardened Saffron' theme in `theme.ts` with optimized typography weights, expanded spacing tiers, and sophisticated border opacities (8%).
- Re-architected `SelectionSheet.tsx` to fully utilize THEME tokens, replacing all hardcoded hex values and blue accents.
- Standardized selection modal geometry with `THEME.radii.inner` and optimized safe-area padding for the bottom action button.
- Integrated `PrimaryButton` into selection modals for high-energy brand consistency.
- Hardened `SettingsToggleCard.tsx` by migrating all text colors and row separators to design system constants.
- Standardized active selection indicators across all commitment configuration pickers (Intensity, Missed Check-ins, etc.).
