feat(backend): Implement Accountability Preset Library with Autonomous Harvest Engine

This update introduces a persistent habit-harvesting system that automatically captures and ranks a user's frequently used locations and app-blocklists.

Key features include:
- Added 'locationPresets' and 'digitalCommitmentPresets' tables to the schema.
- Integrated an autonomous harvest engine into 'create' and 'update' task mutations.
- Implemented smart deduplication (coordinate epsilon & sorted app matching).
- Added Discovery API queries for smart suggestions based on recency and popularity.
- Synchronized location naming conventions ('lat'/'lng') across the entire backend stack.
