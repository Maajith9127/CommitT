fix(native): resolve navigation loops and optimize commitment setup history

- Switched penalty and waiver setup confirmations from `router.push` to `router.back()` to prevent redundant summary screen instances.
- Restored standard stack navigation for selection lists to enable granular, one-page-at-a-time backtracking.
- Updated back-gesture interceptors to correctly handle "Leave it" actions via navigation dispatch, ensuring clean stack pops.
- Improved UX by ensuring setup screens are removed from history upon completion rather than bloating the navigation stack.


wsl.exe --shutdown
