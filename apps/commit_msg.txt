style(map): enhance circular geofence Rendering and unify static previews

- Ported high-performance Static Maps API rendering to `LocationPresetPickerModal`, replacing the legacy SDK views for a 90% reduction in RAM usage.
- Refined `createCirclePath` resolution to 40 polygon points for perfectly smooth circular renders on high-DPI displays.
- Increased geofence stroke thickness to `weight:5` to ensure high contrast against complex hybrid/satellite map tiles.
- Unified the geofence visual style across both the Presets Hub and the Location Selection Modal.
