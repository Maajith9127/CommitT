perf(ui): optimize Blocklist editor and streamline navigation flow

- Integrated global `useAppStore` into [BlocklistView](cci:1://file://wsl.localhost/Ubuntu-22.04/home/maajith/mono/apps/native/components/ui/modal/BlocklistView.tsx:51:0-302:2) to enable 
  instant, high-performance application discovery.
- Implemented `FlatList` virtualization in the blocklist editor to 
  ensure smooth scrolling across large app catalogs.
- Enabled edge-to-edge separators via `fullWidthContent` mode for 
  a premium, native-aligned aesthetic.
- Streamlined UX by removing the intermediate confirmation modal when 
  accessing digital commitment settings from Event Details.
- Added a local staging phase for app selections, ensuring changes 
  are only synced to Cloud/SQLite upon explicit user confirmation.
- Resolved strict TypeScript linting errors and removed redundant 
  legacy fetch logic.
