feat(ui): add Lock (Strict Mode) option to event action menu

- Integrated a new "Lock (Strict Mode)" item into the EventDetailModal action menu.
- Positioned the lock option alongside existing management actions for better visibility.


wsl.exe --shutdown
