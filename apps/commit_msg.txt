feat(protocol): implement 'Lightweight Protocol' for digital app blocking

Optimized the accountability engine to handle commitments requiring only digital blocks without redundant checkpoint generation.

- Backend (Core): Implemented checkpoint-skipping in `createOne` and `update` services for tasks with no location requirement.
- Backend (Grading): Updated `runner.ts` to provide a default 'PASS' verdict for instances without checkpoints, while still enforcing reported violations.
- Native UI (Logic): Enhanced status calculation in `useEventDetail` to show 'Proceeded' for successful digital-only tasks.
- Native UI (Details): Refined `EventDetailTime` to display 'Digital Block: Active (Auto-verify)' instead of check-in counts during the task window.
- Native UI (Status): Updated post-event feedback to correctly show 'Completed' in green rather than a red 'Expired' label for success cases.
- Bug Fix (Creation): Resolved a state-handling issue in `time-set.tsx` that was wiping out slot-level conditions during time window edits.
