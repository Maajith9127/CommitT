feat(native): sync penalty and verification data to local sqlite

- Implement SQLite Migration 7 -> 8 to add penalty_json and conditions_json columns.
- Fix "duplicate column" crash bugs in migrations V3-V5 by wrapping ALTER TABLE in try-catches for fresh install robustness.
- Update Task and Instance repositories to persist penalty snapshots and instance verification statuses.
- Achieve 1:1 parity between Convex Cloud and Local DB to support penalty-aware scheduling and offline-first waivers.
