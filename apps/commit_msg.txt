feat(native): implement keyboard-aware preset editing and stable map UX

Refines the location preset editing experience with a premium, keyboard-aware 
interface and improved map interaction stability.

- Integrated Reanimated to elevate the edit panel above the keyboard (matches 
  CaptchaWaiverView pattern).
- Implemented a unified 'Edit Name' input field with a built-in cancel (X) 
  dismissal button for better name-change ergonomics.
- Upgraded UI icons to MaterialCommunityIcons (pencil, magnify, close-circle) 
  with a cleaner, white-color aesthetic (removing circular backgrounds).
- Disabled automatic camera re-centering during map taps and long-presses to 
  ensure the viewport remains steady while adding/adjusting regions.
- Prevented automatic keyboard dismissal during map interactions, allowing 
  users to edit preset names and adjust map locations simultaneously.
- Cleaned up component logic by removing redundant camera fly effects and 
  fixing duplicated variable declarations.
