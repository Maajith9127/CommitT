fix(ui): standardize blocklist error handling with ConfirmationModal

- Replaced raw native `Alert.alert()` calls with `ConfirmationModal` in BlocklistView.tsx for a consistent production UI.
- Unified strict-lock, sync-failure, and critical-crash error paths into a single dynamic acknowledgement modal.
- Removed unused `Alert` import to maintain a clean dependency tree.
