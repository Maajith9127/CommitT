feat(sync): implement Cloud-First Imperial (Forward-Heal) strategy

- Transitioned synchronization engine from "Revert-on-Fail" to "Imperial Forward-Heal"
- Introduced global `HealOverlay` and `useHealStore` for UI gating during recovery loops
- Implemented `while(true)` retry loops in creation (`useCommitTask`) and deletion (`useTaskActions`) sagas
- Added "Total Eradication" logic to deletion paths, purging all related local instances and ghosts
- Integrated manual Delta-Sync ingestion into healing cycles to guarantee eventual consistency
- Verified eventual consistency via chaos testing of SQLite Disk I/O failures
