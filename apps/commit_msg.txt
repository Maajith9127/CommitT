feat(ui): render waived events in success green on schedules

Updated the calendar event mapping logic to treat the 'waived' status 
as a resolved success state.

- Modified `useCalendarEvents.ts` to map `waived` instances to the same 
  success green (`#4CD964`) used for verified completions.
- Improves at-a-glance feedback by clearly distinguishing between 
  penalized (red) and successfully cleared (green) commitments.
