ui: refine instance deletion UX and manual edit visibility

- Updated `EventDetailModal` to use a cleaner, more direct confirmation 
  prompt ("Delete this instance?") and removed redundant sub-text.
- Added visual 'EDITED' status labels to calendar events to distinguish 
  manually moved commitments from automated recurrence.
