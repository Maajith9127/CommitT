feat(native/backend): implement production-grade preset management system

- Finalized contextual 'Three Dots' overflow menus for individual Location and Digital preset cards.
- Integrated anchored `ActionMenu` with precise coordinate-capturing using native `onTouchStart` events.
- Implemented the full deletion workflow, including the `ConfirmationModal` and destructive action safeguards.
- Added visual management states to `VerificationStatusCircle` (vertical dots affordance and red cross indicator).
- Developed the Convex CRUD backend for presets, including owner-only deletion mutations and Smart Sorting usage tracking (`incrementUsage`).
- Synchronized card selection logic with the global Zustand store and the backend’s frequency algorithms to support "most-used" sorting.
- Reinforced UI stability with staggered map rendering and loading skeletons during management transitions.
