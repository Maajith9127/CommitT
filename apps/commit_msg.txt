refactor(ui): modernize EventDetail modal with global app store integration

- Transitioned [BlocklistSection](cci:1://file://wsl.localhost/Ubuntu-22.04/home/maajith/mono/apps/native/components/ui/modal/EventDetailConditions.tsx:302:0-387:2) to use the global `useAppStore` for 
  instant, zero-latency metadata resolution.
- Eliminated redundant native bridge calls and `useEffect` hooks during 
  modal mount by leveraging pre-hydrated state.
- Migrated verification gallery to use file-based `iconUri` paths for 
  OOM-safe rendering of application icons.
- Implemented high-performance reactive `useMemo` for resolving blocked 
  application IDs to names and icons.
- Added production-grade internal documentation for metadata resolution 
  and fallback icon handling.
