ui(sync): standardize title-only aesthetic for all synchronization alerts

- Refactored all error and success modals to utilize a "single-statement" title layout
- Consolidated descriptive messages into the `title` prop and eliminated subtitle/message clutter
- Removed redundant labels ("Forward-Healing", "Overlap Detected", "Shift Failed") in favor of direct status descriptions
- Standardized `HealOverlay` to display dynamic healing progress as the primary bold title
- Hardened `Alert.alert` calls to move descriptive subtext into the primary title argument
- Fixed a structural `SyntaxError` in `BlocklistView.tsx` by properly closing the `processSave` async function
