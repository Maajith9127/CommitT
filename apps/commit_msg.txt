feat(native/blocker): implement granular security audit and lazy-loading optimization

- Refactor `BlockerAccessibilityService` to transition from a blanket Settings app
  ban to a granular Context-Level block.
- Implement `performDeepSecurityAudit` with a dynamic lexical scanner to detect
  and prevent Time Travel, Developer Mode, and Factory Reset bypasses.
- Lift top-level block on `com.android.settings` to allow user access to basic 
  phone utilities (Wi-Fi, Bluetooth) while maintaining strict commitment security.
- Optimize user experience by disabling `lazy` loading for the Schedules tab,
  leveraging background mounting to ensure instantaneous transitions to the
  Calendar UI.
- Add comprehensive production-level documentation and architectural comments 
  across native Kotlin and TypeScript layers.

Impact: Significantly improves overall platform UX without compromising the 
'Fail-Closed' accountability architecture.
