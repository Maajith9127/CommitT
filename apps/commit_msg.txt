style: polish scheduling flow typography and component depth

Finalized the visual refinements for the commitment scheduling UI, focusing on typographic hierarchy and component elevation.

Key Changes:
- Typography Refinement: Resized the "Active Time" screen header to `xxxl` (30px) to restore its premium prominence.
- Time Slot Optimization: Swapped the time-range display in `TimeSlotCard` to `HeaderTitle` in white (`textMain`) and tuned its size to `base` (16px) for better balance.
- Component Fixes: Resolved a ReferenceError by correctly importing `HeaderTitle` into `TimeSlotCard.tsx`.
- Visual Depth: Polished the `VerificationStatusCircle` by thickening its border to 1.5px and applying the new `surfaceLight` background token to create a tactical HUD look.

This completes the visual hardening of the commitment creation flow, ensuring all schedule-related UI is high-contrast and on-theme.
