feat: refine strict mode duration selection with direct shortcuts and custom picker

Direct Duration Access: Updated the setup screen to show "1 Day" and "2 Days" as top-level selectable options for faster configuration.
Custom Duration Picker: Integrated a 

SelectionSheet
 triggered via the "Custom" option, enabling granular control for longer lock periods.
UI Hierarchy: Renamed the settings section to "Lock Duration" and standardized the header component to match the core commitment creation patterns.
Dynamic Labels: Implemented smart row feedback that displays the selected custom value or a checkmark for standard durations.
Design Polish: Maintained the primary blue branding across all interactive elements, including icons, buttons, and selection indicators.
wsl.exe --shutdown
