refactor: implement scalable timezone-aware audit logging for temporal syncs

- Backend (scheduler.ts): Removed hardcoded backend string formatting and timezone overrides. Switched to passing the raw UTC milliseconds (`scheduled_for`) in the audit log metadata.
- Frontend (notifications.tsx): Implemented client-side formatting using `dayjs` to ensure sync logs automatically adjust to the user's local device timezone.
- UI: Refined the date display to the Global Standard format (D MMM YYYY) for improved readability in the history feed.
