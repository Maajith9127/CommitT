feat(native): refine scheduling UI and fix commitment persistence logic

- Refined TimeSlotCard aesthetic with 1px white/20 dividers and increased corner rounding (rounded-3xl).
- Improved legibility by bumping label font size to 14px and value/placeholder text to 16px.
- Enhanced Remove action with a larger 26px icon and improved touch targets.
- Fixed selection/deselection bug in TimeSetScreen by correctly persisting and passing preset IDs through the draft store.
- Optimized picker modal behavior to prevent auto-closing, requiring explicit dismissal via the close button.
- Resolved 'NOT NULL constraint failed' SQLite error by ensuring assigner_id fallbacks to the active session during creation.
- Cleaned up app layout by removing stale route definitions for 'verify' and 'calendar'.
