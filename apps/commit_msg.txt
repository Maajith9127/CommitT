feat(create-commit): implement dynamic penalty waiver preview

- UI: Refactored the Final screen to dynamically display chosen waiver configuration instead of a static placeholder.
- Mapping: Added specialized icon and text mapping for all waiver types (Captcha, Paragraph, Intensity, Run).
- UX: Integrated a fallback prompt to "Choose a Penalty Waiver" if the accountability contract is incomplete.
- Copy: Refined the Captcha subtitle to explicitly state the requirement (e.g., "Solve 20 high noise captchas to waive off the penalty").
- Store: Ensured a perfect sync between configured draft metadata and the final confirmation UI.
