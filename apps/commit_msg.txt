feat(backend/native): implement slot-specific condition overrides and UI refinements

- Modified backend instance generation to use a 'Total Override' strategy: slot-specific conditions now completely replace global task rules instead of merging with them.
- Refined `TimeSlotCard` to only display accountability rules (Location/Digital) if they are explicitly overridden for that specific time window.
- Improved persistence and visibility of existing task drafts by ensuring slot-level conditions are correctly mapped and rendered when returning to the creation flow.
- UI Polish:
  * Renamed "Delete slot" action to "Remove" for better UX.
  * Increased delete icon size to 26px for improved touch targets.
  * Resolved JSX layout syntax errors and extra nesting in `time-set.tsx`.
