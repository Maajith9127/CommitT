feat(native): architect centralized EnforcementModule for permission auditing

- Created `@modules/enforcement-module` to house all hardware/OS permission checks
- Implemented real-time check for "Appear on Top" (Overlay) via Settings.canDrawOverlays
- Centralized Accessibility Service status auditing into EnforcementModule.kt
- Updated usePermissions hook to consume centralized enforcement manifest
- Added build.gradle and expo-module config to ensure inclusion in Android builds
