fix(native): resolve database connection leak in accessibility heartbeat

- Wrapped pulseRunnable vault-read logic in a try/finally block to guarantee closure.
- Moved db?.close() to the finally block to prevent connection leaks during SQL errors.
- Prevents resource exhaustion crashes caused by the 5-second audit heartbeat.
