feat(native): implement double-confirmation & strict-lock guards for blocklist edits

- Integrated 'ConfirmationModal' into the Save workflow with simplified, single-title messaging for high-impact clarity.
- Harmonized 'Cancel/Discard' button styling with semantic Red (#FF3B30) across both the entry and exit modals.
- Enhanced 'BlocklistView' to intercept 'STRICT_LOCK_ACTIVE' errors, providing clear user feedback when commitments are locked.
- Updated the Convex 'instance:update' mutation to persist structural condition changes to the cloud.
- Optimized the synchronization sequence: Cloud Sync (Backend) → Hardware Sync (Local SQLite) for guaranteed enforcer reactivity.
