feat(sync): achieve 100% Saga coverage across all critical write paths

- Migrate Creation (useCommitTask), Deletion (useTaskActions), and Status (useEventDetail) to TripleWriteOrchestrator.
- Secure Location pivots (EventDetailLocation) and Blocklist updates (BlocklistView) with atomic Saga logic.
- Implement 'Hardware-First' enforcement: force immediate Android alarm refresh after all manual edits.
- Harden Hydration Engine against 'Closed Resource' errors during database hot-swaps.
- Inject Chaos Testing fault points into all newly migrated Saga execution blocks.
- Documented full Saga architecture and Split-Brain protection in project_context.md.
