feat(ui): harden commitment setup and settings with design system tokens

- **Permissions**: Migrated status indicators and card surfaces to THEME tokens; standardized background to `pureBlack` for OLED high-fidelity.
- **Penalties & Waivers**: Applied semantic color mapping (danger for penalties, success for waivers) across the selection UI, replacing hardcoded hex values.
- **Digital Forfeit**: Hardened the "Embarrassing Photo" screen with theme-compliant card surfaces and radii.
- **Fixes**: Resolved an image rendering issue in the forfeit setup by restoring standard `Image` components with explicit re-render keys for Convex signed URLs.
- **Visual Polish**: Unified global backgrounds and component borders across all commitment configuration screens.
